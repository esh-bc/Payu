[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn webserver:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --threads 4"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
